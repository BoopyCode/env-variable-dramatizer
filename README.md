# ENV Variable Dramatizer

Turns missing environment variables into dramatic theater instead of silent failures. Run before your app starts to catch missing variables with flair. Usage: `node env-dramatizer.js VAR1 VAR2 VAR3`